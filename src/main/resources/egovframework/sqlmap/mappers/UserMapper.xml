<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user.dao.UserMapper">
  <insert id="insertUser" parameterType="user.dto.JoinRequestDto">
    INSERT INTO TBL_USER (
        user_id, 
        password, 
        nickname,
        role,
        email, 
        created_at
    ) VALUES (
        #{userId}, 
        #{password}, 
        #{nickname},
        #{role},
        #{email}, 
        CURRENT_TIMESTAMP
    );
  </insert>

  <select id="selectBy" resultType="user.vo.UserVO">
    SELECT user_id as userId, 
           password, 
           nickname,
           role,
           email
    FROM TBL_USER
      <where>
        <if test="userId != null">
          AND user_id = #{userId}
        </if>
        <if test="email != null">
          AND email = #{email}
        </if>
    </where>
  </select>
  
  
</mapper>